# Kopiere diese Datei nach .env und passe die Werte an
# NIEMALS die echte .env ins Git einchecken!

# Datenbank
DB_PASSWORD=secret
DB_USER=sticker

# CORS: Domain unter der das Frontend erreichbar ist
CORS_ORIGIN=https://sticker.deine-domain.de
